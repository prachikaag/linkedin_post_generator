# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Environment — never commit your actual keys
.env

# Generated posts are git-tracked (commit them when you publish)
# posts/*.md   ← uncomment to stop tracking generated posts entirely

# memory.json is git-tracked — it prevents duplicate posts across runs
# memory.json  ← uncomment only to reset memory on every fresh clone

# OS
.DS_Store
.claude/scheduled_tasks.lock
