# Core
anthropic>=0.40.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# CLI & terminal output
click>=8.1.0
rich>=13.7.0

# HTTP (used for NewsAPI and direct Notion API fallback)
requests>=2.31.0

# RSS fallback (used when Claude CLI is unavailable)
feedparser>=6.0.10

# Google Trends fallback (used when Claude CLI WebSearch is unavailable)
pytrends>=4.9.2
