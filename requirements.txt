# Core
anthropic>=0.40.0       # Claude API — post generation
feedparser>=6.0.11      # RSS/Atom feed parsing
httpx>=0.27.0           # HTTP client for feed fetching
pyyaml>=6.0.1           # YAML config loading
python-dotenv>=1.0.1    # .env file support

# Optional — Notion publishing
# Install if you want posts pushed to Notion automatically
notion-client>=2.2.1

# Optional — Google Trends (trending keyword discovery)
# If unavailable, falls back to seed terms from config/topics.yaml
pytrends>=4.9.2
