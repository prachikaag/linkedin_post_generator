# ── Required ──────────────────────────────────────────────────────────────
# Install with: pip install -r requirements.txt
PyYAML>=6.0          # reads config/*.yaml
requests>=2.28.0     # calls Anthropic API and fetches RSS feeds

# ── Optional ───────────────────────────────────────────────────────────────
# Google Trends integration (trending keyword discovery).
# If not installed, the pipeline falls back to the seed terms in config/topics.yaml.
pytrends>=4.9.2
